# Copy to .env — .env itself is git-ignored.

# SQLite file, relative to apps/api. Lives under data/, which is git-ignored.
DATABASE_URL="file:./data/budget.db"

# Port the Nest server listens on. The web dev server proxies /api here.
PORT=3000
